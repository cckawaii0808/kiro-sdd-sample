# 快速開始指南

## 使用 Kiro CLI 進行規格驅動開發

### 步驟 1：啟動 Kiro CLI

```bash
cd E:\測試資料\my-project
kiro-cli chat --agent project-dev
```

啟動後會看到歡迎訊息，顯示規格文件位置。

### 步驟 2：啟用實驗性功能

在 Kiro CLI 中執行：
```
/experiment
```

使用方向鍵選擇並啟用以下功能：
- ✅ Knowledge（知識庫）
- ✅ TODO Lists（任務管理）
- ✅ Checkpointing（變更追蹤）
- ✅ Thinking（推理過程）
- ✅ Tangent Mode（分支對話）

### 步驟 3：建立知識庫

```
/knowledge add docs/
/knowledge add specs/
```

這樣 Kiro 就能隨時查詢規格內容。

### 步驟 4：初始化檢查點

```
/checkpoint init
```

開始追蹤檔案變更。

### 步驟 5：填寫專案規格

```
> 幫我開啟 docs/spec.md，我要填寫專案規格
```

根據你的專案需求填寫：
- 專案目標
- 功能需求
- API 設計
- 資料模型
- 技術棧選擇

### 步驟 6：生成開發計畫

```
> 根據 docs/spec.md 的規格，幫我建立一個完整的開發 TODO list，
> 按照以下順序：
> 1. 專案初始化（package.json, 目錄結構）
> 2. 資料模型定義
> 3. 資料庫設定
> 4. API 路由框架
> 5. 核心業務邏輯
> 6. 認證與授權
> 7. 測試
> 8. 文件
```

Kiro 會自動建立 TODO list。

### 步驟 7：開始開發

```
> 開始第一個 TODO
```

Kiro 會：
1. 讀取規格文件
2. 生成符合規格的程式碼
3. 建立必要的檔案
4. 自動建立 checkpoint

### 步驟 8：驗證實作

```
> 檢查目前的實作是否符合 spec.md 中的規格
```

### 步驟 9：查看變更

```
/checkpoint list
/checkpoint diff 1 HEAD
```

### 步驟 10：保存進度

```
/save project-dev-day1
```

## 常見工作流程

### 探索技術方案（不影響主對話）

```
Ctrl+T  # 進入 tangent mode
> 這個功能用 Redis 還是 Memcached 比較好？

# 研究完畢
Ctrl+T  # 返回主對話
```

### 更新規格後同步程式碼

```
> 我更新了 spec.md 的 User API 規格，幫我檢查哪些程式碼需要修改

/knowledge update docs/spec.md
```

### 委派長時間任務

```
> 委派一個任務：為所有 API 端點生成完整的單元測試和整合測試
```

### 回退到之前的狀態

```
/checkpoint list
/checkpoint restore 3
```

### 查看 TODO 進度

```
/todos view
```

### 繼續之前的 TODO

```
/todos resume
```

## 開發技巧

### 1. 保持規格文件更新
每次需求變更時，先更新 `docs/spec.md`，然後讓 Kiro 根據新規格調整程式碼。

### 2. 頻繁建立檢查點
重要功能完成後，檢查 checkpoint：
```
/checkpoint list
```

### 3. 使用 Tangent Mode 研究
遇到技術問題時，用 `Ctrl+T` 進入 tangent mode 研究，不會污染主對話。

### 4. 讓 Kiro 驗證規格符合度
定期詢問：
```
> 檢查目前所有實作是否完全符合 spec.md 的規格
```

### 5. 保存重要對話
完成重要階段後：
```
/save milestone-1-data-model
/save milestone-2-api-implementation
```

## 下一步

1. 填寫 `docs/spec.md` 的專案規格
2. 選擇技術棧並更新 `docs/architecture.md`
3. 讓 Kiro 幫你生成開發計畫
4. 開始逐項實作

祝開發順利！🚀
